<% if @quotes.present? %>
  <% @quotes.each do |quote| %>
    <div
      class='dropdown-list-element'
      data-action='click->analytics--position-new#selectQuote'
      data-quote-id='<%= quote.id %>'
      data-quote-name='<%= quote.security.name[I18n.locale.to_s] %>'
      data-quote-price='<%= quote.price %>'
    >
      <%= "#{quote.security.ticker} (#{quote.security.name[I18n.locale.to_s]}), #{Money.new(quote.price * 100, quote.price_currency).format} (#{quote.security.type})" %>
    </div>
  <% end %>
<% end %>
